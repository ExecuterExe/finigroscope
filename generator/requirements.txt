# Зависимости РАБОТЫ сервиса. Ставятся и локально, и на сервере:
#   python -m pip install -r requirements.txt
#
# Долгое время здесь было написано, что генератор обходится стандартной
# библиотекой, а jsonschema числилась среди инструментов разработчика. Это
# перестало быть правдой в тот момент, когда аудитор модуля начал проверять
# ответ модели по схеме: agents/module_auditor.py импортирует jsonschema при
# загрузке. Локально ошибка не всплывала — пакет уже стоял ради тестов; на
# чистой машине сервис падал бы при первом же импорте.

jsonschema>=4.0    # строгая проверка ответов аудитора по схеме
