Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Werkzeug>=3.0,<4.0
# Миграции схемы. Нужны с тех пор, как хранилище перестало быть эфемерным:
# db.create_all() умеет добавить недостающую ТАБЛИЦУ, но не колонку в
# существующей — без миграций любое новое поле молча не появлялось бы.
alembic>=1.13,<2.0
gunicorn>=21.2
python-docx>=1.1
requests>=2.31
python-dotenv>=1.0
markdown>=3.5